function gerar() {
    const qtd = parseint(document.getElementById("gerar").Value);
var box;
for(let k=0; k<qtd; k++){
    box = document.createElement("box");
    box.innerHTML = K;
    box.setAttribute("class","box");

    document.body.appendChild(divbox);
}
}